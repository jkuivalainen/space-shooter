# Godot 4 generated files
.godot/
*.uid

# Export presets (contain sensitive paths)
export_presets.cfg

# Imported assets (regenerated by Godot)
.import/

# Claude local context
claude.md
.mcp.json
.claude/

# MCP / editor addons (local tooling, not game code)
addons/
